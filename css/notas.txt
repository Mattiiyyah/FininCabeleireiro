* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    background: black;
    font-family: sans-serif;
}

/* Header fixo e estilizado */
header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 2px solid white;
    padding: 10px 50px;
    background: black;
    z-index: 10;
}

/* Logo */
.logo img {
    width: 100px;
}

/* Navegação */
header nav {
    display: flex;
    align-items: center;
}

header nav a {
    margin: 0 1em;
    color: white;
    text-decoration: none;
    font-weight: bold;
    transition: 0.3s;
}

header nav a:hover {
    color: red;
    border-bottom: 1px solid red;
    text-shadow: 0 0 10px red;
}

/* Container principal com imagem e degradê */
.container {
    margin-top: 100px; 
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
    height: 80vh;
    overflow: hidden;
}

/* Box da imagem com overlay */
.box_img {
    width: 100%;
    height: 100%;
    position: relative;
}

.box_img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
}

/* Degradê na parte inferior da imagem */
.box_img::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 50%;
    background: linear-gradient(to bottom, transparent, black);
}
